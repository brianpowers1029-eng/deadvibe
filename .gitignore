# Python
__pycache__/
*.py[cod]

# Local runtime data
feedback.db

# Downloaded source caches for scripts/build_shows_db.py
data/cache/

# Environment
.env

# shows.db IS committed — it's the runtime catalog of ~2k GD dates.
# Rebuild with: python3 scripts/build_shows_db.py
data/cache/
